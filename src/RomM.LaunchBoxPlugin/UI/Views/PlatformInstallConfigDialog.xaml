<UserControl x:Class="RomMbox.UI.Views.PlatformInstallConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:RomMbox.UI.Converters"
        Background="#141822"
        MinWidth="760"
        MinHeight="720">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <converters:EnumEqualsToBoolConverter x:Key="EnumEqualsToBool" />

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="BorderBrush" Value="#2D374B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A6EDC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#283042"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="#181E2B"/>
            <Setter Property="BorderBrush" Value="#2B3345"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20,18"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="BannerCard" TargetType="Border" BasedOn="{StaticResource SectionCard}">
            <Setter Property="Padding" Value="18,14"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EEF2FF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SectionDescription" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#95A0B8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#DCE4F7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InlineHelper" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#8E99B1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#EEF2FF"/>
            <Setter Property="Background" Value="#111726"/>
            <Setter Property="BorderBrush" Value="#313A4F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="MinHeight" Value="34"/>
        </Style>

        <Style x:Key="BrowseButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Width" Value="104"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style x:Key="StatusBadgeBorder" TargetType="Border">
            <Setter Property="Background" Value="#143B30"/>
            <Setter Property="BorderBrush" Value="#1F6651"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>

        <Style x:Key="CalloutBorder" TargetType="Border">
            <Setter Property="Background" Value="#161C2A"/>
            <Setter Property="BorderBrush" Value="#313A4F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>

        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#2D374B"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#283042"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2D374B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="Background" Value="#111726"/>
            <Setter Property="BorderBrush" Value="#313A4F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="34"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Bd"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>

                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>

                            <ToggleButton Grid.Column="1"
                                          Background="#111726"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1,0,0,0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"
                                          ClickMode="Press">
                                <Border Background="#111726"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1,0,0,0"
                                        CornerRadius="0,6,6,0">
                                    <TextBlock Text="▾"
                                               Foreground="#AAB4C8"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Border Background="#141822"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        BorderBrush="#313A4F"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,6,0,0"
                                        Padding="6">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A6EDC"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#6D8BFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#141822"
                BorderBrush="#2D374B"
                BorderThickness="0,0,0,1"
                Padding="24,18">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="{Binding PageTitle}" FontSize="18" FontWeight="Bold" Foreground="#EEF2FF" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding PageSubtitle}" Style="{StaticResource InlineHelper}"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Back" Width="108" Height="40" Margin="0,0,12,0" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButton}"/>
                    <Button Content="Save" Width="108" Height="40" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" IsEnabled="{Binding CanSave}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Background="#181D28">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          CanContentScroll="False"
                          Margin="0,0,0,92"
                          Padding="24,18,24,24">
                <StackPanel>
                    <Border Style="{StaticResource BannerCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="28"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Selected Platform" Foreground="#7F8AA6" FontSize="11" Margin="0,0,0,3"/>
                                <TextBlock Text="{Binding BannerPlatformLabel}" Foreground="#EEF2FF" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Visibility="{Binding ShowBannerEmulatorSelector, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Emulator" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding Emulators}"
                                          SelectedItem="{Binding SelectedEmulator, Mode=TwoWay}"
                                          DisplayMemberPath="Name"
                                          Style="{StaticResource DarkComboBox}"
                                          IsEnabled="{Binding IsAssociatedEmulatorEnabled}"/>
                            </StackPanel>

                            <Border Grid.Column="3"
                                    Style="{StaticResource StatusBadgeBorder}"
                                    Margin="14,22,0,0"
                                    VerticalAlignment="Top"
                                    Visibility="{Binding ShowBannerStatusBadge, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="{Binding BannerStatusText}" Foreground="#76E0B7" FontWeight="SemiBold"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource SectionCard}" Visibility="{Binding ShowPlatformOptionsSection, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <TextBlock Text="Platform Options" Style="{StaticResource SectionTitle}"/>
                            <TextBlock Text="{Binding PlatformOptionsDescription}" Style="{StaticResource SectionDescription}"/>

                            <TextBlock Text="Games Directory" Style="{StaticResource FieldLabel}"/>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding GamesDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseFolder_Click"/>
                            </Grid>
                            <TextBlock Text="Relative paths are used when the directory is on the same drive as LaunchBox." Style="{StaticResource InlineHelper}" Margin="0,0,0,2"/>
                            <TextBlock Text="Games directory is required." Style="{StaticResource InlineHelper}" Foreground="#FF6E6E" Visibility="{Binding IsGamesDirectoryValid, Converter={StaticResource InverseBoolToVisibility}}" Margin="0,0,0,14"/>

                            <TextBlock Text="Game Layout" Style="{StaticResource FieldLabel}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <RadioButton Content="Install in Root"
                                             Margin="0,0,18,0"
                                             IsChecked="{Binding IsInstallInRootLayout, Mode=TwoWay}"/>
                                <RadioButton Content="Install in Subdirectory"
                                             IsChecked="{Binding IsInstallInSubdirectoryLayout, Mode=TwoWay}"/>
                            </StackPanel>
                            <TextBlock Text="Choose whether games install directly in the platform folder or inside a game-specific subdirectory." Style="{StaticResource InlineHelper}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionCard}" Visibility="{Binding ShowEmulatorOptionsSection, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <TextBlock Text="Emulator Options" Style="{StaticResource SectionTitle}"/>
                            <TextBlock Text="{Binding EmulatorOptionsDescription}" Style="{StaticResource SectionDescription}"/>

                            <TextBlock Text="RetroArch Core" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowRetroArchCoreSelectionField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBox Text="{Binding EmulatorCoreName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}" Visibility="{Binding ShowRetroArchCoreSelectionField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="Enter the RetroArch core name used for this platform, such as snes9x." Style="{StaticResource InlineHelper}" Margin="0,6,0,12" Visibility="{Binding ShowRetroArchCoreSelectionField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="RPCS3 Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowRpcs3ExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,6" Visibility="{Binding ShowRpcs3ExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Rpcs3ExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseRpcs3Executable_Click"/>
                            </Grid>
                            <TextBlock Text="Required to install PKG files and launch via RPCS3." Style="{StaticResource InlineHelper}" Margin="0,0,0,12" Visibility="{Binding ShowRpcs3ExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="RPCS3 License Directory" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowRpcs3LicenseDirectoryField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,6" Visibility="{Binding ShowRpcs3LicenseDirectoryField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Rpcs3LicenseDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseRpcs3License_Click"/>
                            </Grid>
                            <TextBlock Text="Optional override for RPCS3 dev_hdd0/home/00000001/exdata." Style="{StaticResource InlineHelper}" Margin="0,0,0,12" Visibility="{Binding ShowRpcs3LicenseDirectoryField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="External PKG Extractor" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,6" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Ps4ExternalPkgExtractorPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePs4ExternalExtractor_Click"/>
                            </Grid>
                            <TextBlock Text="Required only for direct .pkg download workflow when no built-in PKG installer is available." Style="{StaticResource InlineHelper}" Margin="0,0,0,10" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="Target Extracted File Type" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBox Text="{Binding TargetImportFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="Optional extracted file target used by the PS4 workflow." Style="{StaticResource InlineHelper}" Margin="0,6,0,12" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}"/>

                            <CheckBox Content="Fail if direct PKG extractor is missing"
                                      IsChecked="{Binding Ps4FailIfDirectPkgExtractorMissing, Mode=TwoWay}"
                                      Margin="0,0,0,8"
                                      Visibility="{Binding ShowPs4FailIfDirectPkgExtractorMissingField, Converter={StaticResource BoolToVisibility}}"/>

                            <Border Style="{StaticResource CalloutBorder}" Visibility="{Binding ShowPs4ExternalPkgExtractorPathField, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="PKG extractor configuration is not required for all PS4 installs. It is only needed for workflows involving direct .pkg content." Style="{StaticResource InlineHelper}"/>
                            </Border>

                            <TextBlock Text="PCSX2 Executable" Style="{StaticResource FieldLabel}" Margin="0,12,0,6" Visibility="{Binding ShowPcsx2ExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowPcsx2ExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Pcsx2ExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePcsx2Executable_Click"/>
                            </Grid>

                            <TextBlock Text="PSP Emulator Mode" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowPspEmulatorModeField, Converter={StaticResource BoolToVisibility}}"/>
                            <ComboBox Style="{StaticResource DarkComboBox}"
                                      SelectedValue="{Binding PspEmulatorMode, Mode=TwoWay}"
                                      SelectedValuePath="Content"
                                      Visibility="{Binding ShowPspEmulatorModeField, Converter={StaticResource BoolToVisibility}}"
                                      Margin="0,0,0,12">
                                <ComboBoxItem Content="StandalonePPSSPP" />
                                <ComboBoxItem Content="RetroArchPPSSPP" />
                            </ComboBox>

                            <TextBlock Text="PPSSPP Executable">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowPpssppExecutablePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspStandaloneMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Grid Margin="0,0,0,12">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowPpssppExecutablePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspStandaloneMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding PpssppExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePpssppExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="RetroArch Executable">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowRetroArchExecutablePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspRetroArchMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Grid Margin="0,0,0,12">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowRetroArchExecutablePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspRetroArchMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding RetroArchExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseRetroArchExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="RetroArch PPSSPP Core">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowRetroArchPpssppCorePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspRetroArchMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBox Text="{Binding RetroArchPpssppCorePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource DarkTextBox}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowRetroArchPpssppCorePathField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspRetroArchMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                            <CheckBox Content="Validate RetroArch PPSSPP Assets"
                                      IsChecked="{Binding ValidateRetroArchPpssppAssets, Mode=TwoWay}"
                                      Margin="0,0,0,8">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding ShowValidateRetroArchPpssppAssetsField}" Value="True"/>
                                                    <Condition Binding="{Binding IsPspRetroArchMode}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>

                            <CheckBox Content="Fail If Emulator Not Ready"
                                      IsChecked="{Binding FailInstallIfEmulatorNotReady, Mode=TwoWay}"
                                      Margin="0,0,0,12"
                                      Visibility="{Binding ShowFailInstallIfEmulatorNotReadyField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="Vita3K Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowVita3kExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowVita3kExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding Vita3kExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseVita3kExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="Eden Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowSwitchEdenExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowSwitchEdenExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding SwitchEdenExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseSwitchEdenExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="Azahar Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowAzaharExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowAzaharExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding AzaharExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseAzaharExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="AzaharPlus Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowAzaharPlusExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowAzaharPlusExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding AzaharPlusExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseAzaharPlusExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="Dolphin Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowDolphinExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowDolphinExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding DolphinExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseDolphinExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="Cemu Executable" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowCemuExecutablePathField, Converter={StaticResource BoolToVisibility}}"/>
                            <Grid Margin="0,0,0,12" Visibility="{Binding ShowCemuExecutablePathField, Converter={StaticResource BoolToVisibility}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding CemuExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseCemuExecutable_Click"/>
                            </Grid>

                            <TextBlock Text="Supported File Formats" Style="{StaticResource FieldLabel}" Visibility="{Binding ShowSupportedFileTypesField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBox Text="{Binding SupportedFileTypes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}" Visibility="{Binding ShowSupportedFileTypesField, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Text="Comma-separated extensions used for candidate discovery and emulator handoff." Style="{StaticResource InlineHelper}" Margin="0,6,0,12" Visibility="{Binding ShowSupportedFileTypesField, Converter={StaticResource BoolToVisibility}}"/>

                            <TextBlock Text="No emulator-specific configuration is required for the selected emulator." Style="{StaticResource InlineHelper}" Visibility="{Binding ShowEmulatorOptionsSection, Converter={StaticResource InverseBoolToVisibility}}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionCard}" Visibility="{Binding ShowExtraConfigurationSection, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <TextBlock Text="Extra Configuration" Style="{StaticResource SectionTitle}"/>
                            <TextBlock Text="{Binding ExtraConfigurationDescription}" Style="{StaticResource SectionDescription}"/>

                            <StackPanel Visibility="{Binding ShowPs3GameDirectoryField, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="PS3 Games Directory Override" Style="{StaticResource FieldLabel}"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding Ps3GameDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                    <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePs3Games_Click"/>
                                </Grid>
                                <TextBlock Text="Override the PS3 install directory when using RPCS3." Style="{StaticResource InlineHelper}" Margin="0,0,0,12"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding ShowPs4GamesDirectoryField, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="PS4 Games Directory Override" Style="{StaticResource FieldLabel}"/>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding Ps4GamesDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                    <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePs4Games_Click"/>
                                </Grid>
                                <TextBlock Text="Optional serial-based install root used specifically for PS4 content." Style="{StaticResource InlineHelper}" Margin="0,0,0,12"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding ShowInstallationTypeSection, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Installation Type" Style="{StaticResource FieldLabel}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                    <RadioButton Content="Basic"
                                                 Margin="0,0,18,0"
                                                 IsChecked="{Binding InstallationType, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=Basic}"/>
                                    <RadioButton Content="Enhanced"
                                                 IsChecked="{Binding InstallationType, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=Enhanced}"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Visibility="{Binding ShowBasicOptionsSection, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Basic Workflow" Style="{StaticResource FieldLabel}"/>
                                <CheckBox Content="Self-Contained" IsChecked="{Binding SelfContained, Mode=TwoWay}" Margin="0,0,0,6"/>
                                <TextBlock Text="When enabled, the extracted file is used directly as the ROM path." Style="{StaticResource InlineHelper}" Margin="24,0,0,10"/>

                                <StackPanel IsEnabled="{Binding IsTargetFilesEnabled}" Margin="0,0,0,12">
                                    <TextBlock Text="Target File(s)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding TargetImportFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                    <TextBlock Text="Separate entries with commas (example: eboot.bin, default.xex)." Style="{StaticResource InlineHelper}" Margin="0,6,0,0"/>
                                    <TextBlock Text="{Binding TargetImportFilesSummary}" Style="{StaticResource InlineHelper}" Margin="0,3,0,0"/>
                                </StackPanel>

                                <CheckBox Content="Enable General Fallback Installer" IsChecked="{Binding UseGeneralFallbackInstaller, Mode=TwoWay}" Margin="0,0,0,12" Visibility="{Binding ShowUseGeneralFallbackInstallerField, Converter={StaticResource BoolToVisibility}}"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding ShowEnhancedOptionsSection, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Enhanced Workflow" Style="{StaticResource FieldLabel}"/>

                                <TextBlock Text="Installation Mode" Style="{StaticResource FieldLabel}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <RadioButton Content="Manual"
                                                 Margin="0,0,18,0"
                                                 IsChecked="{Binding InstallerMode, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=Manual}"/>
                                    <RadioButton Content="Automatic"
                                                 IsChecked="{Binding InstallerMode, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=AutoInnoSilent}"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,0,12" IsEnabled="{Binding IsInstallModeAutomatic}">
                                    <TextBlock Text="Custom Silent Install Arguments" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding InstallerSilentArgs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                    <TextBlock Text="Uses default install options when left empty." Style="{StaticResource InlineHelper}" Margin="0,6,0,0"/>
                                </StackPanel>

                                <CheckBox Content="Install Bonus Content" IsChecked="{Binding InstallBonus, Mode=TwoWay}" Margin="0,0,0,6"/>
                                <StackPanel Margin="24,0,0,12" IsEnabled="{Binding IsBonusOptionsEnabled}">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <RadioButton Content="Centralized Install"
                                                     Margin="0,0,18,0"
                                                     IsChecked="{Binding BonusInstallLocation, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=Centralized}"/>
                                        <RadioButton Content="Install in Game Folder"
                                                     IsChecked="{Binding BonusInstallLocation, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=GameFolder}"/>
                                    </StackPanel>
                                    <Grid Margin="0,0,0,4" IsEnabled="{Binding IsBonusCentralizedSelected}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding BonusRootPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                        <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseBonusRoot_Click"/>
                                    </Grid>
                                    <TextBlock Text="Game folder installs create a Bonus Content directory in the installation directory." Style="{StaticResource InlineHelper}"/>
                                </StackPanel>

                                <CheckBox Content="Extract Pre-Requisites" IsChecked="{Binding InstallPreReqs, Mode=TwoWay}" Margin="0,0,0,6"/>
                                <StackPanel Margin="24,0,0,12" IsEnabled="{Binding IsPreReqsOptionsEnabled}">
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding PreReqsRootPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                        <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowsePreReqsRoot_Click"/>
                                    </Grid>
                                    <TextBlock Text="{Binding PreReqsTargetSummary}" Style="{StaticResource InlineHelper}"/>
                                </StackPanel>

                                <CheckBox Content="Install Soundtracks" IsChecked="{Binding InstallOst, Mode=TwoWay}" Margin="0,0,0,6"/>
                                <StackPanel Margin="24,0,0,12" IsEnabled="{Binding IsOstOptionsEnabled}">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <RadioButton Content="Centralized Install"
                                                     Margin="0,0,18,0"
                                                     IsChecked="{Binding OstInstallLocation, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=Centralized}"/>
                                        <RadioButton Content="Install in Game Folder"
                                                     IsChecked="{Binding OstInstallLocation, Converter={StaticResource EnumEqualsToBool}, ConverterParameter=GameFolder}"/>
                                    </StackPanel>
                                    <Grid Margin="0,0,0,4" IsEnabled="{Binding IsOstCentralizedSelected}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding MusicRootPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBox}"/>
                                        <Button Grid.Column="1" Content="Browse" Style="{StaticResource BrowseButton}" Click="BrowseMusicRoot_Click"/>
                                    </Grid>
                                    <TextBlock Text="Game folder installs create a Soundtracks directory in the installation directory." Style="{StaticResource InlineHelper}"/>
                                </StackPanel>
                            </StackPanel>

                            <CheckBox Content="Install DLC Automatically" IsChecked="{Binding InstallDlcAutomatically, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowSkipRegionMismatchedDlcField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Install Updates Automatically" IsChecked="{Binding InstallUpdatesAutomatically, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowSkipRegionMismatchedDlcField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Skip Region-Mismatched DLC" IsChecked="{Binding SkipRegionMismatchedDlc, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowSkipRegionMismatchedDlcField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Skip Unmatched RAP Files" IsChecked="{Binding SkipUnmatchedRapFiles, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowSkipUnmatchedRapFilesField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Prefer Metadata-Based Package Matching" IsChecked="{Binding PreferMetadataBasedPackageMatching, Mode=TwoWay}" Margin="0,0,0,12" Visibility="{Binding ShowPreferMetadataBasedPackageMatchingField, Converter={StaticResource BoolToVisibility}}"/>

                            <CheckBox Content="Install Vita Updates Automatically" IsChecked="{Binding VitaInstallUpdatesAutomatically, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowVitaInstallUpdatesAutomaticallyField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Install Vita DLC Automatically" IsChecked="{Binding VitaInstallDlcAutomatically, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowVitaInstallDlcAutomaticallyField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Consolidate Game Installs" IsChecked="{Binding VitaConsolidateGameInstalls, Mode=TwoWay}" Margin="0,0,0,8" Visibility="{Binding ShowVitaConsolidateGameInstallsField, Converter={StaticResource BoolToVisibility}}"/>
                            <CheckBox Content="Fail If Vita Emulator Not Ready" IsChecked="{Binding VitaFailIfEmulatorNotReady, Mode=TwoWay}" Margin="0,0,0,0" Visibility="{Binding ShowVitaFailIfEmulatorNotReadyField, Converter={StaticResource BoolToVisibility}}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
