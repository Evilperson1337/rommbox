<UserControl
    x:Class="RomMbox.UI.Views.ImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:RomMbox.UI.Converters"
    xmlns:models="clr-namespace:RomMbox.Models.Import"
    mc:Ignorable="d"
    Width="800">

    <!-- ===================== -->
    <!-- Local dark-mode styles -->
    <!-- ===================== -->
    <UserControl.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
        <Style x:Key="DarkCheckBox" TargetType="CheckBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Width="16" Height="16" VerticalAlignment="Center">
                                <Border x:Name="Box"
                                        Background="Transparent"
                                        BorderBrush="#5B6575"
                                        BorderThickness="1"
                                        CornerRadius="2"/>
                                <Path x:Name="CheckMark"
                                      Data="M 3 8 L 7 12 L 13 4"
                                      Stroke="#C8CFDD"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Square"
                                      StrokeEndLineCap="Square"
                                      Visibility="Collapsed"/>
                            </Grid>
                            <ContentPresenter Grid.Column="1"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Box" Property="Opacity" Value="0.5"/>
                                <Setter TargetName="CheckMark" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <converters:EnumEqualsToBoolConverter x:Key="EnumEqualsToBoolConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>

        <Style x:Key="ImportGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#141822"/>
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="BorderBrush" Value="#283042"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#141822"/>
                    <Setter Property="BorderBrush" Value="#283042"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#151A26"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <converters:SkipSuffixVisibilityConverter x:Key="SkipSuffixVisibilityConverter"/>

        <DataTemplate x:Key="EnumDescriptionTemplate">
            <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
        </DataTemplate>

        <!-- Header checkbox -->
        <Style x:Key="HeaderCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource DarkCheckBox}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Cell checkbox -->
        <Style x:Key="CellCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource DarkCheckBox}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="16" Height="16">
                            <Border x:Name="Box"
                                    Background="Transparent"
                                    BorderBrush="#5B6575"
                                    BorderThickness="1"
                                    CornerRadius="2"/>
                            <Path x:Name="CheckMark"
                                  Data="M 3 8 L 7 12 L 13 4"
                                  Stroke="#C8CFDD"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Square"
                                  StrokeEndLineCap="Square"
                                  Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Box" Property="Opacity" Value="0.5"/>
                                <Setter TargetName="CheckMark" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark secondary button (Refresh) -->
        <Style x:Key="DarkSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="BorderBrush" Value="#2D374B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A6EDC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#283042"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DARK ComboBox item -->
        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#283042"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#28468C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DARK ComboBox -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#E6EBF5"/>
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="BorderBrush" Value="#2D374B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Bd"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"/>

                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>

                            <ToggleButton Grid.Column="1"
                                          Background="#1C2230"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1,0,0,0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"
                                          ClickMode="Press">
                                <Border Background="#1C2230"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1,0,0,0"
                                        CornerRadius="0,4,4,0">
                                    <TextBlock Text="▾"
                                               Foreground="#AAB4C8"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Border Background="#141822"
                                        Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        BorderBrush="#2D374B"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,6,0,0"
                                        Padding="6">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A6EDC"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#6D8BFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ProgressBar -->
        <Style x:Key="DarkProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="12"/>
            <Setter Property="Background" Value="#1C2230"/>
            <Setter Property="Foreground" Value="#4A6EDC"/>
            <Setter Property="BorderBrush" Value="#2D374B"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

    </UserControl.Resources>

    <!-- ===================== -->
    <!-- Layout -->
    <!-- ===================== -->
    <StackPanel>

        <TextBlock Text="Games" Style="{StaticResource PageHeader}" Foreground="#FF7F7F7F" FontWeight="Bold"/>
        <TextBlock Text="Select games to import, download, and/or sync saves from the RomM server."
                   Style="{StaticResource HelperText}"
                   Margin="0,0,0,14"/>

        <!-- Top controls -->
        <StackPanel Margin="0,0,0,14">

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Platform" Style="{StaticResource HelperText}" Margin="0,0,14,0"/>
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding RomMPlatforms}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedRomMPlatform}"
                          Width="260">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox" BasedOn="{StaticResource DarkComboBox}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Refresh"
                            Width="110"
                            Command="{Binding RefreshCommand}"
                            Margin="0,0,12,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource DarkSecondaryButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsImportRunning}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <CheckBox Content="Hide Skipped Games"
                              IsChecked="{Binding HideSkipped, Mode=TwoWay}"
                              Style="{StaticResource DarkCheckBox}"
                              VerticalAlignment="Center"/>
                </StackPanel>

            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Duplicate Match" Style="{StaticResource HelperText}" Margin="0,0,14,0"/>
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding DuplicateMatchOptions}"
                          SelectedItem="{Binding SelectedDuplicateMatchOption}"
                          Width="260"
                          ItemTemplate="{StaticResource EnumDescriptionTemplate}">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox" BasedOn="{StaticResource DarkComboBox}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>
                <CheckBox Grid.Column="2"
                          Content="Allow Duplicates"
                          IsChecked="{Binding AllowDuplicates, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          Margin="18,0,0,0">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox" BasedOn="{StaticResource DarkCheckBox}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>

                <Grid Grid.Column="3" Width="260">
                    <TextBox ToolTip="Filter games"
                             Padding="8,4,26,4"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="&#xE721;"
                               FontFamily="Segoe MDL2 Assets"
                               Foreground="#AAB4C8"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               IsHitTestVisible="False"/>
                </Grid>

            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Default Behavior" Style="{StaticResource HelperText}" Margin="0,0,14,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton Content="Import All"
                                 Margin="0,0,16,0"
                                 IsChecked="{Binding BulkAction, Mode=TwoWay, Converter={StaticResource EnumEqualsToBoolConverter}, ConverterParameter=Import}"/>
                    <RadioButton Content="Install All"
                                 Margin="0,0,16,0"
                                 IsChecked="{Binding BulkAction, Mode=TwoWay, Converter={StaticResource EnumEqualsToBoolConverter}, ConverterParameter=Install}"/>
                    <RadioButton Content="Skip All"
                                 IsChecked="{Binding BulkAction, Mode=TwoWay, Converter={StaticResource EnumEqualsToBoolConverter}, ConverterParameter=Skip}"/>
                </StackPanel>
            </Grid>

        </StackPanel>

        <!-- Status + progress -->
        <StackPanel Margin="0,0,0,12">
            <TextBlock Text="{Binding ImportStatusText}"
                       Style="{StaticResource HelperText}"/>
            <ProgressBar Style="{StaticResource DarkProgressBar}"
                         Margin="0,8,0,0"
                         Value="{Binding ImportProgressPercent, Mode=OneWay}"
                         Maximum="100"/>
        </StackPanel>

        <!-- Games grid -->
        <Border BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Background="#141822">
                <DataGrid ItemsSource="{Binding FilteredGames}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                      HeadersVisibility="Column"
                      Height="420"
                      Background="#141822"
                      BorderThickness="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      RowStyle="{StaticResource ImportGridRowStyle}"
                          RowBackground="#141822"
                          AlternatingRowBackground="#151A26"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#283042"
                          VerticalGridLinesBrush="#283042"
                      ScrollViewer.VerticalScrollBarVisibility="Visible"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.CanContentScroll="True">

                    <DataGrid.Style>
                        <Style TargetType="DataGrid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsImportRunning}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Opacity" Value="0.7"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Style>

                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#E6EBF5"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#141822"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#E6EBF5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style x:Key="ImportGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#1C2230"/>
                            <Setter Property="Foreground" Value="#AAB4C8"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderBrush" Value="#2D374B"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ImportGridColumnHeaderStyle}"/>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>

                        <!-- Action -->
                        <DataGridTemplateColumn Header="Action" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.ImportActions, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                              SelectedItem="{Binding Action, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              ItemTemplate="{StaticResource EnumDescriptionTemplate}"
                                              Style="{StaticResource DarkComboBox}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Saves -->
                        <DataGridTemplateColumn Header="Saves" Width="90">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ImportGridColumnHeaderStyle}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource CellCheckBoxStyle}"
                                              Background="Transparent"
                                              IsChecked="{Binding Saves, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource HeaderCheckBoxStyle}"
                                              Content="Saves"
                                              IsChecked="{Binding SavesAll, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.HeaderTemplate>
                        </DataGridTemplateColumn>


                        <!-- Game -->
                        <DataGridTemplateColumn Header="Games" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center">
                                        <Run Text="{Binding Name}" Foreground="#E6EBF5"/>
                                        <InlineUIContainer>
                                            <TextBlock Foreground="#8F9BB3"
                                                       FontStyle="Italic"
                                                       Visibility="{Binding StatusText, Converter={StaticResource SkipSuffixVisibilityConverter}}">
                                                <Run Text=" - "/>
                                                <Run Text="{Binding StatusText}"/>
                                            </TextBlock>
                                        </InlineUIContainer>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
            </DataGrid>
        </Border>

    </StackPanel>
</UserControl>
