<Window x:Class="RomMbox.UI.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RomM"
        Height="220"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="#141822">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/RomMbox;component/UI/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="#141822" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#F5F7FF"
                   TextWrapping="Wrap" />

        <TextBlock x:Name="MessageText"
                   Grid.Row="1"
                   Margin="0,10,0,0"
                   Foreground="#E6EBF5"
                   TextWrapping="Wrap" />

        <TextBlock x:Name="DetailText"
                   Grid.Row="2"
                   Margin="0,8,0,0"
                   Foreground="#AAB4C8"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="No" Width="100" Margin="0,0,8,0" Click="OnNoClick" />
            <Button Content="Yes" Width="100" Style="{StaticResource PrimaryButton}" Click="OnYesClick" />
        </StackPanel>
    </Grid>
</Window>
