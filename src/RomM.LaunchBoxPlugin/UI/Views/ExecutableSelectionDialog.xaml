<Window x:Class="RomMbox.UI.Views.ExecutableSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:RomMbox.UI.ViewModels"
        mc:Ignorable="d"
        Title="Executable Selection" Height="520" Width="920" WindowStartupLocation="CenterOwner"
        Background="#141822">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/RomMbox;component/UI/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Select the executable to launch for this game." FontSize="14" />
            <TextBlock Text="Confirm the highlighted executable or choose a different one." Foreground="#AAB4C8" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Candidates}"
                  SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="None"
                  Background="#141822"
                  RowBackground="#141822"
                  AlternatingRowBackground="#151C2A">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#E6EBF5"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#23406D" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1C2230" />
                    <Setter Property="Foreground" Value="#E6EBF5" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="36" />
                    <Setter Property="Foreground" Value="#E6EBF5" />
                    <Setter Property="Background" Value="Transparent" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1A243A" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#23406D" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsRecommended}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Background" Value="#1B2437" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="2*" />
                <DataGridTextColumn Header="Path" Binding="{Binding DisplayPath}" Width="3*" />
                <DataGridTextColumn Header="Arch" Binding="{Binding Architecture}" Width="90" />
                <DataGridTextColumn Header="Size" Binding="{Binding FileSizeDisplay}" Width="120" />
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="160" />
                <DataGridTextColumn Header="Modified" Binding="{Binding LastModifiedDisplay}" Width="170" />
                <DataGridTemplateColumn Header="" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="#28304A" CornerRadius="4" Padding="6,2"
                                    Visibility="{Binding IsRecommended, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Recommended" FontSize="11" Foreground="#E6EBF5" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="140" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="Confirm Selection" Width="180" Style="{StaticResource PrimaryButton}" Command="{Binding ConfirmCommand}" />
        </StackPanel>
    </Grid>
</Window>
