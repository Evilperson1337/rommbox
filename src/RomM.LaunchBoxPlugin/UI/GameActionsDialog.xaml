<Window x:Class="RomMbox.UI.GameActionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:RomMbox.UI.Converters"
        Title="RomM Game Actions"
        Height="520"
        Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="RomM Game Actions" FontSize="20" FontWeight="Bold" Foreground="#FF7F7F7F"/>
            <TextBlock Text="Manage RomM playback options." Foreground="#AAB4C8"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Title" FontWeight="SemiBold" Foreground="#AAB4C8"/>
            <TextBlock Text="{Binding GameTitle}" FontSize="14" Foreground="White"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <StackPanel Margin="0,0,24,0">
                <TextBlock Text="Platform" FontWeight="SemiBold" Foreground="#AAB4C8"/>
                <TextBlock Text="{Binding PlatformName}" Foreground="White"/>
            </StackPanel>
            <StackPanel Margin="0,0,24,0">
                <TextBlock Text="Release Date" FontWeight="SemiBold" Foreground="#AAB4C8"/>
                <TextBlock Text="{Binding ReleaseDateDisplay}" Foreground="White"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Genres" FontWeight="SemiBold" Foreground="#AAB4C8"/>
                <TextBlock Text="{Binding Genres}" Foreground="White"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="3" BorderBrush="#2D374B" BorderThickness="1" CornerRadius="4" Padding="12" Background="#141822">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="#D5D9E5"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Vertical" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Play on RomM"
                        Width="150"
                        Margin="0,0,8,0"
                        Command="{Binding PlayCommand}"
                        Visibility="{Binding CanPlayOnRomM, Converter={StaticResource BoolToVisibility}}"/>
                <Button Content="Close" Width="120" Command="{Binding CloseCommand}"/>
            </StackPanel>
            <TextBlock Text="{Binding StatusText}" Foreground="#AAB4C8" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Window>
